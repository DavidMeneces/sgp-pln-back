services:

  mealplans-db:
    image: postgres:15-alpine
    container_name: mealplans-db
    environment:
      POSTGRES_DB: sgppln
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
    ports:
      - "5444:5432"
    volumes:
      - mealplans_db_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d
    networks:
      - mealplans-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 30s
      timeout: 10s
      retries: 3

  mealplans-api:
    image: davidmeneces/nurtricenter-mealplans-api:1.0.0
    container_name: mealplans-api
    environment:
      DATASOURCE_URL: jdbc:postgresql://mealplans-db:5432/sgppln
      DATASOURCE_USERNAME: postgres
      DATASOURCE_PASSWORD: admin
    ports:
      - "8081:8081"
    depends_on:
      mealplans-db:
        condition: service_healthy
    networks:
      - mealplans-network
    restart: unless-stopped

volumes:
  mealplans_db_data:

networks:
  mealplans-network:
    driver: bridge
